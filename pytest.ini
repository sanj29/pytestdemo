[pytest]
minversion = 8.0
addopts = -v -ra
testpaths = tests

markers =
    smoke: quick tests to validate core functionality
    regression: full regression test suite
    slow: tests that take longer to execute
